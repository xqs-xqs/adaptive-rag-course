# CLAUDE.md — adaptive-rag-course 生产化重构工程规则

本仓库正在进行一次**分阶段的生产化重构**。你（Claude Code）是实施代理，人类（Felix）是每个阶段的审查者和唯一的推进决策者。

- 唯一权威计划：`docs/REFACTOR_PLAN.md`
- 当前阶段规格：`docs/specs/phase-N.md`（N 由人类指定）
- 本文件的规则优先级高于你自己的任何判断。规则与规格冲突时，停下来问人类。

---

## 会话启动协议（每次新会话必须按顺序执行）

1. 读 `docs/REFACTOR_PLAN.md`，确认阶段状态表中当前处于哪个阶段。
2. 读人类指定的 `docs/specs/phase-N.md` 全文。
3. 执行规格中的【第 0 节：前置假设核对】——逐条用命令验证，把每条的验证结果（命令 + 原样输出摘要）写下来。
   - **任何一条假设不成立 → 立即停止**，把偏差写入 `docs/reports/phase-N-report.md` 的「假设偏差」一节，向人类报告并等待决定。不允许"我觉得影响不大，继续"。
4. 全部假设通过后，输出一份 **≤ 15 行的执行计划**（要动哪些文件、预计的 commit 序列、预计新增依赖），**等人类确认后再写第一行代码**。

## 硬性规则（违反任何一条 = 本阶段失败，需回滚重来）

- **R1 范围锁定**：只允许修改当前阶段规格「涉及文件」中列出的文件。执行中发现规格外的文件必须改动才能完成任务 → 停止，报告原因，等人类批准后才能扩大范围。
- **R2 禁止顺手修改**：规格范围外发现的 bug、坏味道、优化机会，一律**只记录**到 `docs/BACKLOG.md`（一行一条：文件、行号、问题、建议阶段），**不许动手改**。
- **R3 分支纪律**：一个阶段一个分支，命名 `refactor/phase-N-<slug>`（slug 见各规格）。禁止直接提交 `main`。**禁止在人类没有明确说"开始阶段 N"的情况下开始任何阶段的工作**——即使你认为上一阶段已经完美收尾。
- **R4 提交纪律**：Conventional Commits，英文 subject（如 `refactor(retrieval): extract reranker behind interface`）。每个 commit 后项目必须可启动、已有测试必须通过。禁止一个 500 行的巨型 commit。
- **R5 依赖纪律**：新增任何依赖前，先在执行计划中列出：为什么需要、备选是什么、为什么选它。写入 `requirements.txt` 时固定版本号（`==`）。禁止引入重型框架（DI 容器、K8s、Kafka、微服务拆分——见 REFACTOR_PLAN 的非目标）。
- **R6 禁止篡改验收**：不得修改验收阈值、不得删改评测集（`eval` 数据、golden 用例）、不得跳过/注释/删除失败的测试来让门禁变绿。评测指标下跌 → 如实报告，等人类决定。这是最严重的违规。
- **R7 密钥纪律**：不读取、不打印、不提交 `.env` 内容。代码和文档中不得出现任何真实 API key。单元测试禁止真实网络调用（Phase 2 起由 pytest-socket 强制）。
- **R8 卡住协议**：同一个错误连续尝试 2 次仍失败 → 停止。写清楚：试了什么、观察到什么输出、你的两个候选解决方案及各自风险。等人类选择。禁止第 3 次盲试。
- **R9 阶段收尾**：完成后必须 (a) 逐条执行验收标准命令，把**原样输出**贴进 `docs/reports/phase-N-report.md`（模板见 `docs/templates/phase-report-template.md`）；(b) 更新 `docs/REFACTOR_PLAN.md` 状态表；(c) 停止，等人类审查。
- **R10 上下文交接**：会话上下文过长或你感觉开始丢失细节时，先把当前进度、未完成项、下一步写入阶段报告的「交接记录」节，然后建议人类重开会话。

## 验收哲学

**验收标准 = 可执行命令 + 期望输出。形容词不是验收。**
「代码更清晰了」不是验收；「`grep -rn "dashscope" app/services/` 输出为空」是验收。
规格里每条验收标准都附了命令，收尾时逐条跑、逐条贴输出，缺一条就不算完成。

## 常用命令（Phase 0 核对后由你维护更新）

```bash
# 启动服务（待 Phase 0 确认实际入口）
uvicorn app:app --reload          # Phase 1 之后: uvicorn app.main:app --reload

# 冒烟测试（Phase 0 创建）
bash scripts/smoke.sh

# 单元测试（Phase 2 起）
pytest -q

# 静态检查（Phase 2 起）
ruff check . && mypy app/

# 评测（Phase 0 核对实际命令，全量评测会调用真实 API，产生费用，跑之前告知人类）
python evaluation.py --mode full   # Phase 1 之后: python scripts/run_eval.py --mode full
```

## 与人类的分工（不要越界）

- 你负责：按规格实施、跑验收、写报告、记录偏差。
- 人类负责：讲解课和审查课（在 Claude.ai 对话中进行）、批准执行计划、审查 diff、合并 PR、宣布进入下一阶段。
- 这次重构的首要目标是**人类学会每一层**，其次才是代码本身。你跑得快没有奖励；人类看不懂你的 diff 是事故。因此：diff 保持小、commit 信息写清动机、报告里主动标注「这个改动的面试考点是什么」。
